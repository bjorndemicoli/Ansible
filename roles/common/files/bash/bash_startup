# .bash_startup
# This file is used to exec custom command when bash is launched

## LAUNCH TMUX
# if [ "$TMUX" = "" ]; then
    # tmux attach || tmux 
# else
   # echo "" > /dev/null 
    # # custom command if not root
    # # if [ $EUID -ne 0 ]; then 
        # # sudo -i; 
    # # fi 
# fi

# vim: set ts=4 sw=4 tw=0 ft=sh :
